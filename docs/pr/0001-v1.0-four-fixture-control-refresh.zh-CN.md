# PR 说明：v1.0 四个 fixture 控制 artifact 刷新

已在当前源码 revision 重新生成提交中的 provider-free ACM/数学控制矩阵：

- 四个 fixture：Two Sum、扩展欧几里得、区间合并、模逆；
- `single_pass`、`best_of_n`、`orchestrated` 三种策略各重复 3 次（36 条 ledger）；
- 独立 hidden grader 仍开启，并且不会进入模型可见快照；
- manifest 现在固定 `f0bfc4333d16768614350d5ef75c3e55440a43c1`，并记录当前 transcript、sandbox、
  container-image 和 role-model 配置字段；
- 配对比较行现在还包含配对结果说明中新增的精确双侧 McNemar 诊断；
- 控制结果仍为 `single_pass` visible `0/12`，两个增强策略均为 visible/hidden `12/12`。

本次只刷新提交中的 provider-free 证据，不声称真实模型质量。

```text
python -m contextopt agent-eval --fixtures all --repetitions 3 \
  --output experiments/v1.0-acm-math-4-fixtures/report.json \
  --markdown experiments/v1.0-acm-math-4-fixtures/report.md \
  --html experiments/v1.0-acm-math-4-fixtures/report.html \
  --checkpoint experiments/v1.0-acm-math-4-fixtures/checkpoint.json \
  --manifest experiments/v1.0-acm-math-4-fixtures/manifest.json
```

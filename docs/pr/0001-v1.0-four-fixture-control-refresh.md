# PR note: v1.0 four-fixture control artifact refresh

The checked-in provider-free ACM/math control matrix was regenerated at the current source
revision:

- four fixtures: Two Sum, extended GCD, interval merge, and modular inverse;
- three repetitions across `single_pass`, `best_of_n`, and `orchestrated` (36 ledger rows);
- independent hidden graders remain enabled and outside model-visible snapshots;
- the manifest now pins `f0bfc4333d16768614350d5ef75c3e55440a43c1` and records the current transcript,
  sandbox, container-image, and role-model configuration fields;
- paired comparison rows now include the exact two-sided McNemar diagnostic introduced by the
  paired-outcome evidence change;
- the observed control result remains `0/12` visible for `single_pass` and `12/12` visible plus
  hidden success for both stronger strategies.

This refresh changes only checked-in provider-free evidence; it does not claim real-model quality.

```text
python -m contextopt agent-eval --fixtures all --repetitions 3 \
  --output experiments/v1.0-acm-math-4-fixtures/report.json \
  --markdown experiments/v1.0-acm-math-4-fixtures/report.md \
  --html experiments/v1.0-acm-math-4-fixtures/report.html \
  --checkpoint experiments/v1.0-acm-math-4-fixtures/checkpoint.json \
  --manifest experiments/v1.0-acm-math-4-fixtures/manifest.json
```
